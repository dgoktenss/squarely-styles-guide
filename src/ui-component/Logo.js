// material-ui
import { useTheme } from '@mui/material/styles';
import logo from 'assets/images/logo.svg';

const Logo = () => {
    const theme = useTheme();

    return (
        /**
         */
        <h2>Squarely Styles</h2>
    );
};

export default Logo;
